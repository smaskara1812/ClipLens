Django==4.2.16
djangorestframework==3.15.2
drf-spectacular==0.29.0
django-cors-headers==4.4.0
django_celery_results==2.6.0
Pillow==11.2.1
python-dotenv==1.0.1
whitenoise==6.7.0

# PostgreSQL driver (used when USE_POSTGRES=true — default)
psycopg2-binary==2.9.10
pgvector==0.4.2

# MySQL driver (used when USE_MYSQL=true)
#mysqlclient==2.2.4

# MSSQL driver (used when USE_MSSQL=true)
# Requires the ODBC Driver for SQL Server installed on the OS — see README
#mssql-django==1.4
#pyodbc==5.1.0

# Development tools
django-debug-toolbar==4.4.6

# Async task queue
celery==5.6.3
redis==7.4.0
flower==2.0.1

# Image format support — HEIC/HEIF, PSD, WebP, TIFF (large edited files from cameras/editors)
pillow-heif==1.3.0           # HEIC / HEIF (iPhone, Canon HEIC) — pinned; >=1.1 metadata says Pillow>=11 but 10.4 works
# rawpy>=0.23.0              # RAW (CR2, NEF, ARW, DNG) — install separately if needed

# ML / Vision
torch==2.11.0
torchvision==0.26.0
transformers==4.44.2
timm==1.0.26
einops==0.8.2
ultralytics==8.4.33
insightface==0.7.3
onnxruntime==1.24.4
opencv-python==4.13.0.92

# Speaker diarization (optional — only needed if using the "Run Diarization" feature)
# Also requires: HF_TOKEN in .env + accepting terms at https://hf.co/pyannote/speaker-diarization-3.1
pyannote.audio==3.3.2

# Non-speech audio event detection (applause, laughter, music, cheering, crowd,
# booing).  Downloads a ~150 MB CNN14 checkpoint to ~/panns_data/ on first
# task run — no preinstall required.  Runs alongside diarization on a
# dedicated Celery `audio` queue; see start.sh.
panns-inference==0.1.1
soundfile==0.12.1
librosa==0.10.2.post1

faster-whisper==1.1.1
sentencepiece==0.2.0          # required by NLLB-200 tokenizer

gunicorn==25.3.0