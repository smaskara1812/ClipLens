Django==4.2.16
djangorestframework==3.15.2
django-cors-headers==4.4.0
django_celery_results==2.6.0
Pillow==10.4.0
python-dotenv==1.0.1
whitenoise==6.7.0

# PostgreSQL driver (used when USE_POSTGRES=true — default)
psycopg2-binary==2.9.10
pgvector==0.4.2

# MySQL driver (used when USE_MYSQL=true)
#mysqlclient==2.2.4

# MSSQL driver (used when USE_MSSQL=true)
# Requires the ODBC Driver for SQL Server installed on the OS — see README
#mssql-django==1.4
#pyodbc==5.1.0

# Production WSGI server (replaces runserver on Linux)
gunicorn==23.0.0

# Development tools
django-debug-toolbar==4.4.6

# Async task queue
celery==5.6.3
redis==7.4.0
flower==2.0.1

# ML / Vision
torch==2.11.0
torchvision==0.26.0
transformers==4.44.2
timm==1.0.26
einops==0.8.2
ultralytics==8.4.33
insightface==0.7.3
onnxruntime==1.24.4
opencv-python==4.13.0.92
gunicorn==23.0.0
